

// int arrLength = arr.length;
// maxRight = arr[length-1];

// arr[length-1] = -1;

// for(int i = arr.length - 2; i > 0; i--) {

    // int temp = arr[i];
    // arr[i] = maxRight;

    // if(temp > maxRight) {
        // temp = maxRight;


    // }

// }